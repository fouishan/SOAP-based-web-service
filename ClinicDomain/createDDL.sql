CREATE TABLE PATIENT (ID BIGINT NOT NULL, DOB DATE, NAME VARCHAR(255), PATIENTID BIGINT, PRIMARY KEY (ID));
CREATE TABLE Treatment (ID BIGINT NOT NULL, TreatmentType VARCHAR(31), DIAGNOSIS VARCHAR(255), patient_fk BIGINT, provider_fk BIGINT, PRIMARY KEY (ID));
CREATE TABLE PROVIDER (NPI BIGINT NOT NULL, SPECIALIZATION VARCHAR(255), NAME VARCHAR(255), PRIMARY KEY (NPI));
CREATE TABLE DRUGTREATMENT (ID BIGINT NOT NULL, DOSAGE FLOAT, DRUG VARCHAR(255), PRIMARY KEY (ID));
CREATE TABLE SURGERY (ID BIGINT NOT NULL, SURGERYDATE DATE, PRIMARY KEY (ID));
CREATE TABLE RADIOLOGY (ID BIGINT NOT NULL, PRIMARY KEY (ID));
CREATE TABLE Radiology_DATES (Radiology_ID BIGINT, DATES DATE);
ALTER TABLE Treatment ADD CONSTRAINT FK_Treatment_provider_fk FOREIGN KEY (provider_fk) REFERENCES PROVIDER (NPI);
ALTER TABLE Treatment ADD CONSTRAINT FK_Treatment_patient_fk FOREIGN KEY (patient_fk) REFERENCES PATIENT (ID);
ALTER TABLE DRUGTREATMENT ADD CONSTRAINT FK_DRUGTREATMENT_ID FOREIGN KEY (ID) REFERENCES Treatment (ID);
ALTER TABLE SURGERY ADD CONSTRAINT FK_SURGERY_ID FOREIGN KEY (ID) REFERENCES Treatment (ID);
ALTER TABLE RADIOLOGY ADD CONSTRAINT FK_RADIOLOGY_ID FOREIGN KEY (ID) REFERENCES Treatment (ID);
ALTER TABLE Radiology_DATES ADD CONSTRAINT FK_Radiology_DATES_Radiology_ID FOREIGN KEY (Radiology_ID) REFERENCES Treatment (ID);
CREATE TABLE SEQUENCE (SEQ_NAME VARCHAR(50) NOT NULL, SEQ_COUNT DECIMAL(38), PRIMARY KEY (SEQ_NAME));
INSERT INTO SEQUENCE(SEQ_NAME, SEQ_COUNT) values ('SEQ_GEN', 0);
